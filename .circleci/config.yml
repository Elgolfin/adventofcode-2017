version: 2
jobs:
  build:
    docker:
      - image: golang:latest
    working_directory: ~/adventofcode-2017
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          name: "Run unit tests"
          command: |
            cd hello
            go test